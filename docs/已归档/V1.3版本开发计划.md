# V1.2.0开发计划

## 主要任务：

- 继续修改知识库功能页面的各区域
  - 对左侧目录区域进行收尾工作
  - 重点修改中间编辑区域，使其实现基础的笔记记录功能

- [x] ## 任务1：准备工作

- **左侧目录区域**：减少宽度，使其变窄。

- **中间区域**：顶部删掉标签显示；删掉带有“显示源码编辑器”按钮的顶部栏

# V1.3.0开发计划

## 主要目标：

做成类似Typora那种的基础富文本编辑器

- [x] ## 任务1：实现中间区域可编辑

- **实现中间区域可实时编辑**：当前中间区域只能显示创建时的文件标题，要编辑文件必须在源代码编辑器中进行编辑，这样非常不方便

- **整理后端已开发功能**：同时，之前在后端中已经开发了一些与mrakdown编辑相关的功能，请你整理，看看哪些功能还没有支持前端

# V1.3.1开发计划

## 主要任务：

- 优化基础编辑体验

- 针对中间区域，进行WebUI结构重构，并添加更多功能


- [x] ## 任务1：

- **笔记末尾内容自动留空：**

  > - ## 功能名称
  >
  >   **笔记末尾自动留空（Auto Trailing Blank Line）**
  >
  >   ## 触发条件
  >
  >   1. 当前光标位于**文档最后一个字符所在的行**（即文档末尾内容的最后一行）。
  >   2. 用户按下 **Enter 键**。
  >
  >   ## 预期行为
  >
  >   假设当前文档内容最后一行是 L，光标在 L 行末尾：
  >
  >   | 步骤 | 行为                                               |
  >   | :--- | :------------------------------------------------- |
  >   | 1    | 正常执行 Enter 操作：在 L 行下方创建一个**新行 N** |
  >   | 2    | 光标移到 N 行（此时 N 行为空）                     |
  >   | 3    | **立即且自动**在 N 行下方**再创建一个新的空行 M**  |
  >   | 4    | 光标**仍然停留在 N 行**（不是 M 行）               |
  >
  >   ### 最终效果
  >
  >   text
  >
  >   ```
  >   ……（原有正文）
  >   L（最后一行原有内容）
  >   N（空行，光标在此）
  >   M（空行，不包含光标）
  >   ```
  >
  >   
  >
  >   用户在视觉上看到：文档末尾始终**保留一个空白行**（M 行）。
  >   下一次在末尾按 Enter 时，应**重复上述过程**，始终保持仅 1 个末尾空行（不会无限增加空行）。
  >
  >   ## 特殊规则
  >
  >   - 如果文档当前末尾**已经有一个空白行**：
  >     - 光标在该空白行时，按 Enter 应**仅新增一个空行**（不额外再创建第二个空行），避免空行堆积。
  >   - 如果文档完全为空：
  >     - 不触发该功能，或仅创建一个空行（以编辑器默认行为为准）。
  >
  >   ## 与普通 Enter 的区别
  >
  >   | 场景                 | 普通编辑器 | 本功能                           |
  >   | :------------------- | :--------- | :------------------------------- |
  >   | 光标不在末尾         | 只新增一行 | 只新增一行（不变）               |
  >   | 光标在末尾（内容行） | 新增一行   | 新增**两行**，光标停在第一行空行 |
  >   | 光标在末尾空行       | 新增一行   | 新增一行（不翻倍）               |

- **实现上下文感知的 Enter 行为**（和Typora同逻辑）

  > - 在结构化块（列表项、代码块行、引用行）中，若当前行为空，按 Enter 则退出该块（转为普通段落）；
  > - 否则按 Enter 继续当前块类型（新建列表项 / 代码块新行 / 引用新行）；
  > - 当光标位于普通段落且上一行为某结构化块时，按 Enter 自动创建对应类型的空块成员（如新列表项）。

- [x] ## 任务2：中间区域最顶部新增标签栏

- ### 多文件标签栏

  这一层类似浏览器式标签页，适合多文件显示的场景，建议支持这些能力：

  #### 基础能力

  - 打开多个笔记标签
  - 点击切换标签
  - 关闭标签
  - 关闭其他标签
  - 拖动调整顺序

  #### 建议补充

  - 未保存时显示小圆点 `●`(未实现，之后再补)
  - 当前标签高亮
  - 鼠标悬停显示完整路径
  - 右键标签弹出菜单

  比如：

  ```
  test.md   计算机网络.md   概率论笔记.md ●
  ```

  右键标签可出现：

  ```
  关闭
  关闭其他
  复制路径
  ```

  这个体验会很像 VS Code / 浏览器，用户会很顺手。

# V1.3.2

## 主要目标：

- 中间区域第二层新增菜单功能栏

- 这个地方“不要把所有功能按钮摊开”，建议做成**文字菜单栏**。


- 同时这里集成了各种核心功能，开发难度较大，建议分阶段开发


------

- [x] ### 任务1：文件

负责“文档级操作”

建议放：

- 新建笔记
- 新建文件夹
- 保存
- 另存为
- 重命名
- 导出 
  - 导出为Markdown
  - 导出为 PDF（已实现，但是稍微有点不顺，点击后默认打开浏览器自带的打印功能）


------

- [x] ### 任务2：编辑

负责“基础编辑行为”

建议放：

- 撤销
- 重做
- 剪切
- 复制
- 粘贴
- 查找
- 替换
  - **问题**：查找下一个的快捷键enter和创建下一行的这个行为冲突了（未解决）

- 全选

------

- [x] ### 任务3：段落

负责“块级结构”

建议放：

- 一级标题
- 二级标题
- 三级标题
- 无序列表
- 有序列表
- 引用块
- 分割线
- 代码块
- 任务列表（未实现，稍后再补）

这个菜单很适合知识笔记。

------

- [x] ### 任务4：格式

负责“行内样式”

建议放：

- 加粗
- 斜体
- 删除线
- 行内代码
- 超链接（未实现，之后再补）
- 图片（未实现，之后再补）
- 表格（未实现，之后再补）
- 高亮（未实现，之后再补）

------

- [x] ### 任务5：视图

这个会非常重要。

建议放：

- 阅读模式
- 编辑模式
- 专注模式
- 显示/隐藏右侧辅助区
- 显示/隐藏左侧目录区
- 显示源码编辑器

你之前那个“显示源码编辑器”按钮，就很适合归到这里。

# V1.3.3

## 主要目标：

- 解决一些很影响体验感的小问题

- [x] ## 任务1：粘贴markdown格式的文本时直接转化为最终的效果

- 我复制了AI生成的带有markdown源码的文本，然后粘贴到文中，居然显示的是带有源码文本而不是最终带有格式的文本

  - **bug：**复制代码块时会在上方创建一个空白代码块

- [x] ## 任务2：撤销功能优化

- 这个撤销功能好像只能撤销我的打字，但是对于调整格式和设置或者我复制粘贴的行为，都无法撤销

- [x] ## 任务3：添加快捷键功能

- 设置一个快捷键的按钮，暂时放在功能栏中（这个按钮是临时的），点击可弹出一个快捷键设置的界面，可在其中设置所有功能按钮的快捷键

- 设置比较常用的快捷键：复制、粘贴、全选、n级标题、无序和有序列表、加粗。这些就和Typora中的快捷键一致。还有就是按tab缩进

- 剩下的一些快捷键，暂时不设置。但是你给我列出来这些功能推荐的快捷键，和Typora保持一致

# V1.3.4

## 主要目标：

- 主要解决编辑区剩下的一些小bug和待优化的问题

- [x] ## 任务1：Tab键

  - 设置全局Tab键为缩进，shift + Tab为取消缩进
  - 注意：由于项目在浏览器中显示，因此浏览器的Tab快捷键会影响到该项目Tab键

- [x] ## 任务2：段落新增正文文本H0

  - 段落的菜单栏中新增H0，快捷键同样设置为ctrl + 0

- [x] ## 任务3：修复快捷键问题

  - 新增：按两下快捷键为取消格式。比如按ctrl + 1设置为一级标题，那么再按ctrl + 1撤销设置
  - 无序列表和有序列表的两个快捷键无法正常使用，按了没反应
    - 无序和有序列表的快捷键最好设置为ctrl + shift + }和ctrl + shift + {

- [x] ## 任务4：修复格式菜单编码错误问题

  - 格式菜单栏中第一个功能（即加粗上一个功能）编码错误，出现乱码。请你修复

# V1.3.5

## 主要目标：

- 解决视图功能中的部分问题

- [x] ## 任务1：修复阅读模式

  - 视图中的阅读模式下，会显示部分样式的源码：
    - 例如：分割线显示***、换行有的会显示\<br />、表格显示不正常

- [x] ## 任务2：修复专注模式

- [x] ## 任务3：修复“隐藏左右侧区域”

  - 点击隐藏左侧目录或者右侧辅助区时会出现严重的界面布局混乱

# V1.3.6

## 主要目标：

- 编辑区：新增更多功能

- 该版本主要是新增编辑区与目录区联动的功能

- [x] ## 任务1：Markdown 导入

  - POST /api/knowledge/notes/import-markdown
  - POST /api/knowledge/notes/import-markdown-batch
  - 目标：修复上述问题，确保前端点击后可真实导入markdown，并且将按钮从“视图”中移到“文件”菜单栏中
  
- [x] ## 任务2：收藏能力

  - POST /api/knowledge/notes/:id/favorite
  - 现状：后端支持，当前 Typora 主编辑区没有收藏功能入口。
  - 目标：
    - 在“文件”菜单栏中添加一个“收藏笔记”的按钮，并且确保点击后可收藏到左侧目录区域的收藏栏中
    - 同时在鼠标右键点击笔记弹出的菜单栏中也新增收藏笔记的按钮

- [x] ## 任务3：回收站 / 恢复

  - DELETE /api/knowledge/notes/:id
  - POST /api/knowledge/notes/:id/restore
  - 目标：
    - 首先在“文件”菜单栏中新增一个删除按钮
    - 前端接入后端的软删除和恢复功能，确保文件删除后会显示在左侧目录区域的回收站栏中
    - 回收站栏中右击文件可弹出“恢复笔记”的选项，即前端支持恢复功能
    - 支持30天后自动删除功能（可选，若该功能好实现或者后端已有类似的功能的话就实现，若实现困难可暂时不做）

# 当前版本遗留问题：

- [x] 功能：任务列表未实现
- [x] V1.3.2的任务1：导出为 PDF（已实现，但是稍微有点不顺，点击后默认打开浏览器自带的打印功能）
- [x] V1.3.2的任务2：查找下一个的快捷键enter和创建下一行的这个行为冲突了（未解决）
- [x] V1.3.2的任务3：任务列表（未实现，稍后再补）
- [ ] V1.3.2的任务4：
  - [ ] 超链接（未实现，之后再补）
  - [x] 图片（未实现，之后再补）
  - [x] 表格（未实现，之后再补）
  - [ ] 高亮（未实现，之后再补）
- [ ] V1.3.3的任务3：快捷键设置面板以及所有功能快捷键的设置
- [ ] V1.3.6的任务3：30天后自动删除功能暂未支持



