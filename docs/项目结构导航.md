# 项目结构导航

> 本文档是项目生命周期内持续维护的代码定位地图。它不替代开发规范和设计文档，而是帮助开发者和 AI Agent 快速判断“某类改动应该去哪里改、不要去哪里改”。

## 维护原则

1. 新增一级目录、重要模块目录或跨模块入口时，必须更新本文档。
2. 文件职责发生迁移时，更新对应目录说明和“改动入口”。
3. 本文档只描述稳定边界、关键文件和定位规则，不逐函数罗列实现细节。
4. 普通业务源文件理想不超过 250 行；超过 250 行需说明是否职责过多，超过 400 到 500 行且职责混杂时应列入拆分队列。
5. 自动生成文件、bundle、mock 数据、schema、路由配置等可以较长，但不能混入业务逻辑。
6. 当前仍在重构中，若本文档与代码不一致，以代码为准，并在本次改动中修正文档。

## 根目录

| 路径 | 作用 | 维护说明 |
| --- | --- | --- |
| `apps/` | 应用入口集合，当前包含 API 与 Web。 | 业务代码优先在这里定位。 |
| `packages/` | workspace 共享包。 | 跨应用复用逻辑放这里；当前还很轻。 |
| `docs/` | 规范、设计、计划、结构导航。 | 架构决策和长期说明写这里。 |
| `scripts/` | 开发脚本和构建脚本。 | 只放工程自动化，不放业务逻辑。 |
| `prisma/` | Prisma schema 预留/数据库结构定义。 | 当前不是主运行路径，改动前确认实际使用情况。 |
| `storage/` | 根级运行数据、上传文件、临时文件。 | 属于本地运行数据，不应承载源码逻辑。 |
| `node_modules/` | 依赖安装目录。 | 不手改。 |

## API 应用

`apps/api` 是 Node.js HTTP API，主要负责知识库数据、附件、搜索、知识点等后端能力。

### `apps/api/src`

| 路径 | 作用 | 关键文件 |
| --- | --- | --- |
| `app.factory.js` | 创建应用上下文，组装模块与基础设施。 | API 测试常从这里验证模块装配。 |
| `main.js` | API 启动入口。 | 负责启动，不应写业务逻辑。 |
| `server.js` | HTTP server 与路由入口。 | 当前仍偏大，是后端后续拆分重点。 |
| `README.md` | API 源码说明。 | 与本文档互补。 |

### `apps/api/src/config`

配置解析与运行参数。

| 文件 | 职责 |
| --- | --- |
| `env.schema.js` | 环境变量 schema 和默认值。 |
| `storage.config.js` | 存储路径与本地优先存储配置。 |

### `apps/api/src/infrastructure`

跨业务模块基础设施。

| 文件 | 职责 |
| --- | --- |
| `file-data-store.js` | JSON 文件数据存储读写、导入导出。 |
| `local-attachment-store.js` | 本地附件上传、读取、删除与元数据管理。 |

### `apps/api/src/modules/knowledge`

知识库核心模块，采用近似 DDD 的分层：`domain -> application -> infrastructure -> http`。

| 路径 | 作用 |
| --- | --- |
| `domain/` | 领域实体与领域规则，不依赖 HTTP 和存储细节。 |
| `application/` | 应用服务，编排领域对象与 repository。 |
| `application/dto/` | 创建/更新输入 DTO 构造与默认值处理。 |
| `infrastructure/` | Repository 实现，面向数据存储。 |
| `http/` | HTTP handler，负责请求到应用服务的适配。 |
| `index.js` | 知识库模块装配出口。 |

当前主要文件：

| 文件 | 职责 |
| --- | --- |
| `domain/note.js` | 笔记实体、正文解析、内部链接等领域规则。 |
| `domain/folder.js` | 文件夹实体。 |
| `domain/tag.js` | 标签实体。 |
| `domain/knowledge-space.js` | 知识空间实体。 |
| `domain/knowledge-point.js` | 知识点实体与片段关系。 |
| `application/note-service.js` | 笔记创建、更新、删除、恢复、收藏、导入等业务。 |
| `application/folder-service.js` | 文件夹树、移动、删除子树等业务。 |
| `application/tag-service.js` | 标签创建、更新、删除与列表。 |
| `application/search-service.js` | 笔记搜索。 |
| `application/knowledge-space-service.js` | 知识空间默认创建与列表。 |
| `application/knowledge-point-service.js` | 知识点创建、更新、来源片段维护、软删除。 |
| `application/knowledge-point-tag-groups.js` | 知识点内置标签组。 |
| `http/knowledge-handlers.js` | 知识库 HTTP handler 聚合。 |

维护规则：

- HTTP 层不要直接访问 repository。
- application 层不要依赖 HTTP 请求/响应对象。
- domain 层不要依赖 storage、HTTP、环境变量。
- 新增知识库实体时，优先按 `domain/application/infrastructure/http/test` 补齐。

### `apps/api/src/presentation`

| 文件 | 职责 |
| --- | --- |
| `markdown-preview.js` | Markdown 预览渲染能力。 |

### `apps/api/test`

API 测试目录。

| 文件类型 | 作用 |
| --- | --- |
| `*.domain.test.js` | 领域规则测试。 |
| `*-dto.test.js` | DTO 构造与默认值测试。 |
| `*-repository.test.js` | repository 存取测试。 |
| `*-service.test.js` | 应用服务测试。 |
| `*-http.test.js` / `server-routes.test.js` | HTTP handler 和路由行为测试。 |
| `run-tests.js` | 默认 `npm test` 聚合入口。 |

### `apps/api/storage`

API 本地存储目录。

| 路径 | 作用 |
| --- | --- |
| `data/knowledge-base.json` | API 本地知识库数据。 |
| `uploads/` | API 附件上传文件。 |

## Web 应用

`apps/web` 是当前主要前端工作台。历史上存在 `app/`、`components/` 的 React/Next 风格结构，但当前主运行路径仍以 `src/main.js + src/client.js + lib/*` 为主。

### `apps/web/src`

| 文件/目录 | 作用 | 状态 |
| --- | --- | --- |
| `main.js` | Web shell、静态资源、API 代理、首屏数据注入。 | 仍需自查职责，后续可拆 shell/server/proxy。 |
| `client.js` | 浏览器主交互入口。 | 当前仍是最大上帝文件，重构主战场。 |
| `styles.css` | 入口样式。 | 与 `styles/` 协同。 |
| `services/` | 前端 API service 层。 | 已从 `client.js` 拆出。 |

`src/services`：

| 文件 | 职责 |
| --- | --- |
| `api-client.js` | 统一 `requestJson`，兼容旧 `{ data }` 与新成功 envelope，统一错误消息。 |
| `api-response.js` | `asArray`、`asItems`、`getData` 等响应归一化工具。 |
| `knowledge-api.js` | 知识库 API 调用封装。文件刚好 250 行，继续新增时优先拆子 service。 |

维护规则：

- `client.js` 不应直接拼 `/api/...` URL，应走 `src/services`。
- service 层只处理请求、响应归一化和 endpoint 选择，不操作 DOM。
- 新增 API 先补 service 测试，再接 UI。

### `apps/web/lib`

前端可测试业务/视图逻辑库。这里是当前重构后主要沉淀位置。

| 文件 | 职责 |
| --- | --- |
| `markdown.js` | Markdown 预览与标题提取。 |
| `mock-knowledge-base.js` | 前端本地演示数据，属于 mock 数据例外文件。 |
| `mock-workspace.js` | 将 mock seed 转为本地工作区状态。 |
| `tree-workspace.js` | 文件夹树与笔记树结构操作：树构建、扁平化、目录增删改移、笔记插入/重命名/删除/移动、笔记视觉类型判断。 |
| `tree-name-validation.js` | 目录/笔记命名冲突校验。 |
| `workspace-cache.js` | 工作区缓存读写与首屏快照读取。 |
| `workspace-loading.js` | 启动加载恢复策略、快照合并、首屏注入脚本。 |
| `workspace-normalization.js` | 外部工作区数据归一化：目录树、笔记默认字段、无效节点过滤、更新笔记集合。 |

### `apps/web/lib/editor`

编辑器相关纯逻辑、工具与 Milkdown 集成。

| 文件 | 职责 |
| --- | --- |
| `milkdown-entry.js` | Milkdown 编辑器入口与集成逻辑。当前较大，后续需要继续观察。 |
| `milkdown-bundle.js` / `.css` / `.map` | 构建产物，自动生成/打包文件例外，不手改。 |
| `enhanced-image-block.js` | 图片块增强逻辑，当前较大，后续应检查是否还能拆。 |
| `enter-behavior.js` | Enter/Tab/Backspace 等编辑行为解析。 |
| `markdown-paste.js` | Markdown 粘贴处理。 |
| `image-block-sizing.js` | 图片尺寸计算。 |
| `file-menu.js` | 文件菜单命名、同级名称收集、Markdown 导入条目/标题/状态文案、复制笔记输入、导出文件名与导出 HTML 文档生成等纯逻辑。 |
| `find-navigation.js` | 查找面板导航行为。 |
| `draft-state.js` | 编辑器草稿保存状态判断、本地草稿 note 构造。 |
| `editor-panel-state.js` | 编辑器面板状态计算。 |
| `editor-panel-renderers.js` | 编辑器查找/替换面板 HTML 渲染与状态文案。 |
| `preview-renderers.js` | Markdown 预览面板、源码编辑面板 HTML 渲染。 |
| `view-state.js` | 编辑器渲染模式决策：空状态、回收站、预览/源码、富文本复用与挂载。 |
| `view-renderers.js` | 编辑器视图宿主 HTML 渲染，例如 Milkdown host、工具面板和表格弹窗挂载点。 |
| `tab-renderers.js` | 已打开笔记标签栏 HTML 渲染：空状态、激活/保存中/拖拽状态 DOM hook。 |
| `scroll-positions.js` | 编辑器滚动位置捕获与缓存读写。 |
| `shortcut-actions.js` | 编辑器快捷键动作解析。 |
| `menu-renderers.js` | 编辑器顶部菜单 HTML 渲染：文件、段落、编辑、格式、视图菜单及菜单项快捷键显示。 |
| `save-indicator.js` | 编辑器保存状态到用户可见文案的映射。 |
| `table-dialog-renderers.js` | 表格插入弹窗输入归一化与 HTML 渲染。 |
| `context-menu-model.js` | 编辑器右键菜单动作分组与动作文案/图标元数据。 |
| `context-menu-icons.js` | 编辑器右键菜单 SVG 图标渲染。 |
| `context-menu-renderers.js` | 编辑器右键菜单 HTML 渲染：快捷操作行、标题/插入子菜单、快捷键显示。 |
| `tab-menu-renderers.js` | 笔记标签页右键菜单 HTML 渲染。 |
| `tab-workspace.js` | 打开标签页、关闭、重排逻辑。 |

### `apps/web/lib/knowledge-points`

知识点前端功能模块。

| 文件 | 职责 |
| --- | --- |
| `panel.js` | 知识点面板总装入口。 |
| `panel-renderers.js` | 知识点卡片、筛选栏、加入已有面板渲染。 |
| `panel-utils.js` | 面板转义、标签 map 等工具。 |
| `filtering.js` | 知识点过滤。 |
| `form.js` | 知识点编辑表单读取与字段归一化。 |
| `selection.js` | 从编辑器选区构造知识点/来源片段输入。 |
| `state.js` | 知识点集合更新、当前笔记来源片段选择、本地知识点聚合创建、本地来源片段增删规则。 |

维护规则：

- 面板渲染只返回 HTML 字符串，不改全局 state。
- `form.js` 只读取传入表单并返回更新字段，不提交 API、不操作全局 state。
- `selection.js` 不直接访问 DOM，应只处理传入的 selection snapshot。
- 与后端通信走 `src/services/knowledge-api.js`。

### `apps/web/lib/folders`

文件夹状态与输入构造模块。

| 文件 | 职责 |
| --- | --- |
| `state.js` | 本地文件夹输入构造。 |

维护规则：

- `state.js` 只构造文件夹字段，不操作文件夹树、不读写全局 state、不调用 API。
- 文件夹树增删改移仍在 `apps/web/lib/tree-workspace.js`。

### `apps/web/lib/notes`

笔记集合状态规则模块。

| 文件 | 职责 |
| --- | --- |
| `state.js` | 本地笔记输入构造，以及笔记集合更新：重命名、软删除、永久删除、恢复、清空回收站、收藏、移动目录。 |

维护规则：

- `state.js` 只接收显式参数并返回新笔记输入或新集合，不读写 `client.js` 全局 state、不操作 DOM、不调用 API。
- 文件夹树结构操作仍在 `apps/web/lib/tree-workspace.js`；笔记自身字段状态更新优先放在 `apps/web/lib/notes/state.js`。

### `apps/web/lib/navigation`

左侧导航、目录树与回收站视图的可测试模块。

| 文件 | 职责 |
| --- | --- |
| `tree-renderers.js` | 左侧分组、笔记节点、回收站节点、内联编辑行、删除确认行、空状态项的 HTML 渲染。 |
| `visibility.js` | 导航树可见性选择器：直接子笔记、选中文件夹范围、搜索结果、文件夹搜索命中判断。 |
| `selection.js` | 导航选择与展开状态计算：目录切换后选中笔记、校正无效选择、展开父级目录、切换目录开合、构造目录路径文案。 |
| `drag-drop.js` | 导航树拖拽落点解析与可投放判断：目录/笔记移动、根级材料区落点、自身和子孙目录防护。 |
| `click-target.js` | 导航树点击目标解析：分组开合、目录开合、目录选择、笔记选择。 |
| `tree-editor.js` | 导航树内联编辑命名校验适配：根据创建/重命名模式收集同级目录和笔记，再复用统一命名规则。 |
| `context-menu.js` | 导航树右键菜单模型：目标解析、材料区/目录/笔记/回收站菜单项规则。 |
| `context-menu-renderers.js` | 导航树右键菜单 HTML 渲染：菜单项按钮、分隔线、文本与属性转义。 |
| `section-menu-renderers.js` | 左侧二级分组菜单渲染：收藏、最近、回收站显示开关。 |

维护规则：

- `tree-renderers.js` 只负责生成稳定 DOM hook 和 HTML 字符串，不直接绑定事件、不读写全局 state。
- `visibility.js` 只接收显式输入并返回派生结果，不读取 `client.js` 全局 state。
- `selection.js` 只返回下一步导航状态或路径文案，不执行保存、渲染、网络请求或 DOM 操作。
- `drag-drop.js` 只判断目标和移动合法性，不直接执行移动命令。
- `click-target.js` 只解析点击目标，不执行选择、展开或渲染。
- `tree-editor.js` 只做导航树内联编辑校验所需的同级数据收集和规则调用，不提交创建/重命名命令。
- `context-menu.js` 只解析右键目标和菜单项规则，不打开菜单、不修改选中状态。
- `context-menu-renderers.js` 只渲染菜单 HTML 字符串，不读取全局 state、不操作 DOM。
- `section-menu-renderers.js` 只维护二级分组菜单项和 HTML 字符串，不操作 DOM。
- 左侧树的数据增删改仍归 `apps/web/lib/tree-workspace.js`，渲染入口与事件编排暂时仍由 `apps/web/src/client.js` 承担。
- 修改导航树 HTML 时同步检查 `apps/web/test/navigation-tree-renderers.test.js` 和历史 UI hook 测试；修改可见性规则时同步检查 `apps/web/test/navigation-visibility.test.js`；修改选择/展开规则时同步检查 `apps/web/test/navigation-selection.test.js`；修改拖拽落点规则时同步检查 `apps/web/test/navigation-drag-drop.test.js`；修改点击目标规则时同步检查 `apps/web/test/navigation-click-target.test.js`；修改内联编辑命名校验时同步检查 `apps/web/test/navigation-tree-editor.test.js`；修改右键目标规则时同步检查 `apps/web/test/navigation-context-menu.test.js`；修改右键菜单项规则时同步检查 `apps/web/test/navigation-context-menu-items.test.js`；修改右键菜单 HTML 时同步检查 `apps/web/test/navigation-context-menu-renderers.test.js`；修改二级分组菜单时同步检查 `apps/web/test/navigation-section-menu-renderers.test.js`。

### `apps/web/lib/search`

搜索与标签筛选模块。

| 文件 | 职责 |
| --- | --- |
| `state.js` | 搜索状态判断、标签选择、匹配、标签使用计数、toggle。 |
| `renderers.js` | 顶部搜索框 shell、chips 与搜索下拉面板 HTML 渲染。 |

维护规则：

- 搜索 UI 渲染保持在 `renderers.js`，不要回流到 `client.js`。
- 搜索状态计算保持纯函数，便于测试。

### `apps/web/lib/status`

底部状态栏渲染模块。

| 文件 | 职责 |
| --- | --- |
| `renderers.js` | 底部状态栏指标与数据源模式 HTML 渲染。 |

维护规则：

- 状态栏模块只接收显式状态快照并返回 HTML 字符串，不读取 `client.js` 全局 state、不操作 DOM。
- 新增状态栏字段时优先补充 `apps/web/test/status-renderers.test.js`，再接入 `client.js`。

### `apps/web/lib/shell`

应用外壳与框架级 UI 渲染模块。

| 文件 | 职责 |
| --- | --- |
| `rail-renderers.js` | 左侧模块 Rail 按钮、标签和 SVG 图标 HTML 渲染。 |
| `view-state.js` | 应用视图模式到有效布局状态的纯计算，例如 focus 模式强制隐藏左右侧栏。 |

维护规则：

- shell 模块只承载应用框架级 UI 片段，不混入知识库、编辑器、侧栏等业务面板逻辑。
- Rail 渲染只接收显式模块项列表并返回 HTML 字符串，不读取 `client.js` 全局 state、不操作 DOM。
- 视图状态计算只接收显式 `view` 快照，不操作 DOM；DOM 写入仍由 `client.js` 或后续布局编排模块负责。

### `apps/web/lib/sidebar`

右侧侧栏状态与后续侧栏纯逻辑沉淀位置。

| 文件 | 职责 |
| --- | --- |
| `state.js` | 侧栏相关状态补丁，目前负责清空当前笔记侧栏数据、推导本地侧栏数据、保留或清空知识点编辑态。 |
| `tabs.js` | 右侧侧栏 tab 元数据与当前内容 key 解析规则。 |
| `renderers.js` | 右侧侧栏通用 HTML 片段：tabs、空状态、AI 占位、标签 pill、关联笔记、附件列表。 |
| `stats.js` | 笔记正文统计，目前负责从 Markdown 中计算可读字符数。 |
| `info-panel.js` | 右侧信息面板渲染：笔记元信息、标签 composer、关联笔记与附件区组装。 |
| `outline-panel.js` | 右侧大纲面板渲染：标题列表、空状态和跳转所需 DOM hook。 |

维护规则：

- 只返回可测试的状态补丁，不直接读写 `client.js` 全局 state；本地侧栏数据推导只接收笔记和附件快照。
- tab 元数据和内容选择规则放在 `tabs.js`，不要在 `client.js` 内散落 switch。
- 通用侧栏 HTML 片段放在 `renderers.js`，不要继续堆回 `client.js`。
- 正文统计等纯计算放在 `stats.js`，不要依赖 DOM 或全局 state。
- 信息面板组装放在 `info-panel.js`，调用方只传入显式状态快照和格式化函数。
- 大纲面板组装放在 `outline-panel.js`，只接收已解析的 heading 列表。
- 右侧侧栏 DOM 渲染暂时仍在 `client.js`，后续可继续按 `info/outline/concepts/ai` 拆分。

### `apps/web/lib/tags`

| 文件 | 职责 |
| --- | --- |
| `state.js` | 标签 upsert、删除标签时同步笔记与搜索筛选、生成唯一标签 id、标签名归一化、同名标签查找和新标签输入构造。 |

### `apps/web/styles`

| 文件 | 职责 |
| --- | --- |
| `tokens.css` | 设计 token。新增颜色/阴影/间距优先放这里。 |
| `components.css` | 组件样式集合。当前较大，后续应按模块继续拆分或 token 化。 |

维护规则：

- 不在 JS 动态 HTML 中随意硬编码视觉值。
- 标签颜色等真实运行时数据可以以内联形式出现，但固定视觉规则应进入 CSS。

### `apps/web/test`

前端测试目录，覆盖纯函数、service、源码 hook 和 UI 结构约束。

| 测试类型 | 示例 |
| --- | --- |
| service 测试 | `api-client.test.js`、`knowledge-api.test.js` |
| 纯状态/选择器测试 | `search-state.test.js`、`tag-state.test.js`、`knowledge-point-state.test.js` |
| 渲染字符串测试 | `search-renderers.test.js`、`knowledge-point-panel-renderers.test.js` |
| 编辑器行为测试 | `enter-behavior.test.js`、`editor-scroll-positions.test.js` |
| 历史 UI hook 测试 | `v143-tag-ui.test.js`、`v202-knowledge-point-panel.test.js` |

维护规则：

- 新拆纯函数必须配套测试。
- 旧源码断言应逐步改为检查模块边界，而不是强绑定 `client.js` 内部实现。

### `apps/web/app` 与 `apps/web/components`

这里保留了一套 React/Next 风格页面与组件。

| 路径 | 作用 | 当前状态 |
| --- | --- | --- |
| `app/` | React/Next 页面壳。 | 不是当前主运行路径，改动前确认入口。 |
| `components/` | React 组件。 | 与当前 Vanilla JS 工作台存在并行结构，需要后续统一或归档。 |

维护规则：

- 不要在不了解运行入口的情况下同时改 `src/client.js` 和 `components/*` 实现同一功能。
- 若未来切换到 React 主路径，应单独设计迁移计划。

## 共享包

### `packages/shared`

当前只有包骨架和 README。适合未来放跨 `apps/api` 与 `apps/web` 的纯类型、常量或协议定义。

维护规则：

- 只放真正跨应用复用的稳定内容。
- 不放依赖 DOM、HTTP server 或本地文件系统的实现。

## 脚本

### `scripts`

| 文件 | 职责 |
| --- | --- |
| `build-milkdown-bundle.mjs` | 构建 Milkdown bundle。 |
| `dev-runtime-ports.js` | 开发运行端口管理。 |
| `start-dev.ps1` | Windows 开发启动脚本。 |
| `test/dev-runtime-ports.test.js` | 脚本测试。 |

维护规则：

- 脚本可以操作工程环境，但不要承载业务功能。
- 修改脚本后运行对应脚本测试或最小验证命令。

## 存储目录

### `storage`

根级本地运行数据。

| 路径 | 作用 |
| --- | --- |
| `data/knowledge-base.json` | 根级本地知识库数据。 |
| `uploads/` | 根级上传文件。 |
| `runtime/dev-ports.json` | 开发端口运行时状态。 |
| `exports/` | 导出产物目录。 |
| `temp/` | 临时文件目录。 |

维护规则：

- 存储目录不是源码目录。
- 测试或运行产生的数据改动不要和源码重构混在同一个提交里，除非任务明确要求。

## 当前重构导航

### 已拆出的前端边界

| 能力 | 主要位置 |
| --- | --- |
| API 请求 | `apps/web/src/services/*` |
| 工作区缓存、加载策略与数据归一化 | `apps/web/lib/workspace-cache.js`、`workspace-loading.js`、`workspace-normalization.js` |
| mock 工作区 | `apps/web/lib/mock-workspace.js` |
| 文件夹输入构造 | `apps/web/lib/folders/state.js` |
| 笔记集合状态 | `apps/web/lib/notes/state.js` |
| 搜索与标签筛选 | `apps/web/lib/search/*` |
| 标签状态 | `apps/web/lib/tags/state.js` |
| 底部状态栏渲染 | `apps/web/lib/status/renderers.js` |
| 应用外壳 Rail 渲染 | `apps/web/lib/shell/rail-renderers.js` |
| 应用外壳视图状态计算 | `apps/web/lib/shell/view-state.js` |
| 知识点面板/状态/选区 | `apps/web/lib/knowledge-points/*` |
| 左侧导航树渲染与可见性 | `apps/web/lib/navigation/*` |
| 编辑器滚动缓存 | `apps/web/lib/editor/scroll-positions.js` |
| 编辑器查找/替换面板渲染 | `apps/web/lib/editor/editor-panel-renderers.js` |
| Markdown 预览/源码编辑面板渲染 | `apps/web/lib/editor/preview-renderers.js` |
| 编辑器视图模式决策与宿主渲染 | `apps/web/lib/editor/view-state.js`、`view-renderers.js` |
| 编辑器顶部菜单渲染 | `apps/web/lib/editor/menu-renderers.js` |
| 编辑器右键菜单模型与渲染 | `apps/web/lib/editor/context-menu-*.js` |
| 已打开笔记标签栏渲染 | `apps/web/lib/editor/tab-renderers.js` |
| 笔记标签页右键菜单渲染 | `apps/web/lib/editor/tab-menu-renderers.js` |
| 编辑器保存状态文案 | `apps/web/lib/editor/save-indicator.js` |
| 表格插入弹窗渲染 | `apps/web/lib/editor/table-dialog-renderers.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（搜索） | `apps/web/lib/events/search-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（window） | `apps/web/lib/events/window-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（document-click） | `apps/web/lib/events/document-click-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（document-keyboard） | `apps/web/lib/events/document-keyboard-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（document-input） | `apps/web/lib/events/document-input-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（document-action） | `apps/web/lib/events/document-action-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（menu） | `apps/web/lib/events/menu-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（folder-tree） | `apps/web/lib/events/folder-tree-events.js` |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 事件绑定子域（note-tab） | `apps/web/lib/events/note-tab-events.js` |

### 仍需优先拆分

| 文件/区域 | 问题 | 建议方向 |
| --- | --- | --- |
| `apps/web/src/client.js` | 仍然数千行，混合状态、事件、渲染、命令、编辑器控制。 | 按左侧树、笔记命令、菜单、编辑器生命周期继续拆；事件绑定已迁出到 `apps/web/lib/events/`，本轮已拆 `search-events` + `window-events` + `document-click-events` + `document-keyboard-events` + `document-input-events` + `document-action-events` + `menu-events` + `folder-tree-events` + `note-tab-events`。 |
| `apps/api/src/server.js` | HTTP 入口仍偏胖，混合路由、代理、静态响应等职责。 | 拆 `http/router`、`response`、`request`、route 文件。 |
| `apps/web/styles/components.css` | 样式集中且较大。 | 先 token 化，再按模块拆样式。 |
| `apps/web/lib/editor/milkdown-entry.js` | 编辑器集成较重。 | 区分 host 生命周期、插件配置、命令适配。 |
| `apps/web/lib/editor/enhanced-image-block.js` | 图片块逻辑较大。 | 检查 resize、upload、render 是否可拆。 |
| `apps/web/components/*` | 与当前主运行路径并行。 | 明确保留、迁移或归档策略。 |

## 常见改动入口

| 需求 | 优先定位 |
| --- | --- |
| 新增/修改知识库后端业务 | `apps/api/src/modules/knowledge/application` |
| 新增知识库 HTTP 接口 | `apps/api/src/modules/knowledge/http` 与 `apps/api/src/server.js` 当前路由入口 |
| 修改前端 API 调用 | `apps/web/src/services/knowledge-api.js` |
| 修改搜索筛选规则 | `apps/web/lib/search/state.js` |
| 修改搜索框 shell/下拉/顶部 chip HTML | `apps/web/lib/search/renderers.js` |
| 修改标签状态同步 | `apps/web/lib/tags/state.js` |
| 修改底部状态栏 HTML | `apps/web/lib/status/renderers.js` |
| 修改左侧模块 Rail HTML/图标 | `apps/web/lib/shell/rail-renderers.js` |
| 修改 focus/侧栏显隐等有效视图状态规则 | `apps/web/lib/shell/view-state.js` |
| 修改知识点面板 | `apps/web/lib/knowledge-points/*` |
| 修改左侧目录/导航树 HTML | `apps/web/lib/navigation/tree-renderers.js` |
| 修改左侧目录/导航树筛选与可见性 | `apps/web/lib/navigation/visibility.js` |
| 修改左侧目录/笔记选择与展开状态 | `apps/web/lib/navigation/selection.js` |
| 修改左侧目录/笔记拖拽落点规则 | `apps/web/lib/navigation/drag-drop.js` |
| 修改左侧导航点击目标解析 | `apps/web/lib/navigation/click-target.js` |
| 修改左侧导航内联编辑命名校验 | `apps/web/lib/navigation/tree-editor.js` |
| 修改左侧导航右键菜单目标和菜单项规则 | `apps/web/lib/navigation/context-menu.js` |
| 修改左侧导航右键菜单 HTML | `apps/web/lib/navigation/context-menu-renderers.js` |
| 修改左侧二级分组菜单 HTML | `apps/web/lib/navigation/section-menu-renderers.js` |
| 修改右侧侧栏状态补丁 | `apps/web/lib/sidebar/state.js` |
| 修改右侧侧栏 tab 列表/内容选择规则 | `apps/web/lib/sidebar/tabs.js` |
| 修改右侧信息面板 HTML | `apps/web/lib/sidebar/info-panel.js` |
| 修改右侧大纲面板 HTML | `apps/web/lib/sidebar/outline-panel.js` |
| 修改右侧侧栏通用 HTML 片段 | `apps/web/lib/sidebar/renderers.js` |
| 修改笔记正文统计 | `apps/web/lib/sidebar/stats.js` |
| 修改编辑器顶部菜单 HTML | `apps/web/lib/editor/menu-renderers.js` |
| 修改编辑器右键菜单动作/图标/HTML | `apps/web/lib/editor/context-menu-*.js` |
| 修改已打开笔记标签栏 HTML | `apps/web/lib/editor/tab-renderers.js` |
| 修改笔记标签页右键菜单 HTML | `apps/web/lib/editor/tab-menu-renderers.js` |
| 修改编辑器保存状态文案 | `apps/web/lib/editor/save-indicator.js` |
| 修改编辑器查找/替换面板 HTML | `apps/web/lib/editor/editor-panel-renderers.js` |
| 修改 Markdown 预览/源码编辑面板 HTML | `apps/web/lib/editor/preview-renderers.js` |
| 修改编辑器空/回收站/预览/源码/富文本挂载决策 | `apps/web/lib/editor/view-state.js` |
| 修改编辑器富文本宿主 HTML | `apps/web/lib/editor/view-renderers.js` |
| 修改表格插入弹窗 HTML/行列输入边界 | `apps/web/lib/editor/table-dialog-renderers.js` |
| 修改编辑器快捷键/输入行为 | `apps/web/lib/editor/*` |
| 修改目录树数据操作 | `apps/web/lib/tree-workspace.js` |
| 修改本地文件夹输入构造 | `apps/web/lib/folders/state.js` |
| 修改本地笔记集合状态 | `apps/web/lib/notes/state.js` |
| 修改目录/笔记命名校验 | `apps/web/lib/tree-name-validation.js` |
| 修改视觉 token | `apps/web/styles/tokens.css` |
| 修改组件样式 | `apps/web/styles/components.css` |

## 更新记录

| 日期 | 说明 |
| --- | --- |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 2026-06-25 | 新增 `apps/web/lib/events/search-events.js`，迁出原 `client.js` L449-545 的搜索 shell / 二级导航折叠 / Markdown 导入事件绑定，由 Claude Code 拆分 bindEvents 时新增。 |
| 2026-06-25 | 新增 `apps/web/lib/events/window-events.js`，迁出 `window.beforeunload` 滚动位置持久化，由 Claude Code 拆分 bindEvents 时新增。 |
| 2026-06-25 | 新增 `apps/web/lib/events/document-click-events.js`，迁出原 `client.js` L870-895 的两段 `document.click` 监听器（格式化按钮 + 外部点击关闭菜单链），由 Claude Code 拆分 bindEvents 时新增。 |
| 2026-06-25 | 新增 `apps/web/lib/events/document-keyboard-events.js`，迁出原 `client.js` 的 `document.keydown` 监听器（冒泡阶段 Escape 关闭链 + 捕获阶段 `, true` 编辑器快捷键/表格对话框/查找面板），由 Claude Code 拆分 bindEvents 时新增。 |
| 2026-06-25 | 新增 `apps/web/lib/events/document-input-events.js`，迁出 `client.js` 的 `document.input` 监听器（`#editor-table-dialog` 行/列字段 + `#editor-utility-panel` 查找/替换字段），由 Claude Code 拆分 bindEvents 时新增。 |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 2026-06-25 | 新增 `apps/web/lib/events/document-action-events.js`，迁出 `client.js` 的 `document.click` 监听器（`[data-editor-table-dialog-action]` 表格对话框动作 + `[data-editor-panel-action]` 编辑器面板动作 + `[data-save-now]` 全局保存按钮），由 Claude Code 拆分 bindEvents 时新增。 |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 2026-06-25 | 新增 `apps/web/lib/events/menu-events.js`，迁出 `client.js` 中 `contextMenu.click`（派发 `[data-context-action]`）、`sectionMenu.click`（切换 `[data-section-toggle]`）、`editorMenuBar.click`（切换 `[data-editor-menu-toggle]` + 派发 5 类动作 `[data-{file,edit,paragraph,format,view}-menu-action]`），由 Claude Code 拆分 bindEvents 时新增。 |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 2026-06-25 | 新增 `apps/web/lib/events/folder-tree-events.js`，迁出 `client.js` 中 `elements.folderTree` 上 10 个监听器（2 click + contextmenu + dragstart/dragover/drop/dragend + submit/keydown/input），由 Claude Code 拆分 bindEvents 时新增。 |
<!-- Claude Code 拆分 bindEvents 时新增 -->
| 2026-06-25 | 新增 `apps/web/lib/events/note-tab-events.js`，迁出 `client.js` 中 `elements.noteTabs`（6 个监听器：click / contextmenu / dragstart / dragover / drop / dragend）和 `elements.noteTabMenu`（click）共 7 个监听器（tab 关闭按钮保留 `event.stopPropagation()`），由 Claude Code 拆分 bindEvents 时新增。 |
| 2026-06-25 | 新增 `apps/web/lib/folders/state.js`，并将本地文件夹输入构造、手动/导入笔记输入构造从 `tree-workspace.js` 分别迁入 `folders/state.js` 与 `notes/state.js`，使 `tree-workspace.js` 回到 250 行以内。 |
| 2026-06-25 | 新增 `apps/web/lib/notes/state.js`，将本地笔记重命名、删除、恢复、清空回收站、收藏和移动状态规则从 `client.js` 迁出，并补充 `note-state.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/navigation/selection.js`，将当前笔记目录路径构造从 `client.js` 迁出，并更新 `v140-right-rail-ui.test.js` 的模块边界断言。 |
| 2026-06-25 | 扩展 `apps/web/lib/editor/file-menu.js`，将同级名称收集和本地复制笔记输入构造从 `client.js` 迁出，并补充 `file-menu.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/editor/file-menu.js` 与 `apps/web/lib/tree-workspace.js`，将 Markdown 导入条目读取、导入状态文案、本地导入笔记输入构造从 `client.js` 迁出，并补充相关测试。 |
| 2026-06-25 | 新增 `apps/web/lib/editor/draft-state.js`，将草稿保存变更判断和本地草稿 note 构造从 `client.js` 迁出，并补充 `editor-draft-state.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/tree-workspace.js`，将本地目录/手动笔记输入构造从 `client.js` 迁出，并补充 `tree-workspace.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/sidebar/state.js`，将本地模式侧栏关联笔记/附件推导从 `client.js` 迁出，并补充 `sidebar-state.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/workspace-normalization.js`，将 note 集合替换合并逻辑从 `client.js` 迁出，并补充 `workspace-normalization.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/tags/state.js`，将标签名归一化、同名标签查找、新标签输入构造从 `client.js` 迁出，并补充 `tag-state.test.js`。 |
| 2026-06-25 | 新增 `apps/web/lib/knowledge-points/form.js`，将知识点编辑表单读取和标签 id 合并逻辑从 `client.js` 迁出，并补充 `knowledge-point-form.test.js`。 |
| 2026-06-25 | 新增 `apps/web/lib/workspace-normalization.js`，将目录树/笔记归一化从 `client.js` 迁出，并补充 `workspace-normalization.test.js`。 |
| 2026-06-25 | 将本地知识点聚合创建迁入 `apps/web/lib/knowledge-points/state.js`，让 `client.js` 只负责选择、API/本地分支和渲染编排。 |
| 2026-06-25 | 扩展 `apps/web/lib/knowledge-points/state.js`，将本地知识点来源片段追加/移除规则从 `client.js` 迁出，并补充 `knowledge-point-state.test.js`。 |
| 2026-06-25 | 扩展 `apps/web/lib/editor/file-menu.js`，将 Markdown 导入标题推导和导出 HTML 文档模板从 `client.js` 迁出，并补充 `file-menu.test.js`。 |
| 2026-06-25 | 拆出左侧导航内联编辑命名校验适配模块 `apps/web/lib/navigation/tree-editor.js`，让 `client.js` 只传入状态快照并补充 `navigation-tree-editor.test.js`。 |
| 2026-06-24 | 拆出编辑器渲染模式决策 `apps/web/lib/editor/view-state.js` 与富文本宿主 HTML `apps/web/lib/editor/view-renderers.js`，`client.js` 继续负责 DOM 数据集、host teardown/mount 和辅助面板刷新。 |
| 2026-06-24 | 拆出右侧侧栏 tab 元数据与内容选择规则 `apps/web/lib/sidebar/tabs.js`，`client.js` 继续负责调用对应面板渲染并挂载 DOM。 |
| 2026-06-24 | 拆出应用外壳视图状态计算模块 `apps/web/lib/shell/view-state.js`，`client.js` 继续负责把有效布局状态写入 workspace、sidebar 和 aside DOM。 |
| 2026-06-24 | 将顶部搜索框 shell HTML 合并到 `apps/web/lib/search/renderers.js`，`client.js` 继续负责 DOM 查询、输入值同步、筛选状态和面板挂载。 |
| 2026-06-24 | 拆出左侧模块 Rail 渲染模块 `apps/web/lib/shell/rail-renderers.js`，`client.js` 继续负责 DOM 挂载和模块项状态来源。 |
| 2026-06-24 | 拆出底部状态栏渲染模块 `apps/web/lib/status/renderers.js`，`client.js` 继续负责计算可见笔记数量、目录数量和数据模式状态。 |
| 2026-06-24 | 拆出已打开笔记标签栏渲染模块 `apps/web/lib/editor/tab-renderers.js`，`client.js` 继续负责打开笔记筛选、右键菜单同步、拖拽指示同步和缓存持久化。 |
| 2026-06-24 | 拆出 Markdown 预览面板与源码编辑面板渲染模块 `apps/web/lib/editor/preview-renderers.js`，`client.js` 继续负责 markdown 解析、视图模式选择和编辑器宿主切换。 |
| 2026-06-24 | 拆出表格插入弹窗输入归一化与 HTML 渲染模块 `apps/web/lib/editor/table-dialog-renderers.js`，`client.js` 继续负责弹窗开关、提交和自动聚焦。 |
| 2026-06-24 | 拆出编辑器查找/替换面板渲染模块 `apps/web/lib/editor/editor-panel-renderers.js`，`client.js` 继续负责打开状态、DOM 挂载与自动聚焦。 |
| 2026-06-24 | 拆出笔记标签页右键菜单渲染模块 `apps/web/lib/editor/tab-menu-renderers.js`，`client.js` 继续负责打开、关闭、定位与菜单动作编排。 |
| 2026-06-24 | 拆出编辑器保存状态文案映射模块 `apps/web/lib/editor/save-indicator.js`，`client.js` 只负责将状态写入 DOM。 |
| 2026-06-24 | 拆出编辑器右键菜单动作模型、SVG 图标与 HTML 渲染到 `apps/web/lib/editor/context-menu-*.js`，`client.js` 仅负责显隐、定位和事件编排。 |
| 2026-06-24 | 拆出编辑器顶部菜单渲染模块 `apps/web/lib/editor/menu-renderers.js`，让 `client.js` 只负责传入当前笔记、视图状态、打开菜单 key 与快捷键标签函数。 |
| 2026-06-24 | 将右侧侧栏 tabs 渲染迁入 `apps/web/lib/sidebar/renderers.js`，让 `client.js` 只传入 tab 列表和当前激活 key。 |
| 2026-06-24 | 拆出右侧大纲面板渲染模块 `apps/web/lib/sidebar/outline-panel.js`，保留 `client.js` 只负责解析当前 Markdown 标题并传参。 |
| 2026-06-24 | 拆出右侧信息面板渲染模块 `apps/web/lib/sidebar/info-panel.js`，让 `client.js` 只负责传入当前状态快照。 |
| 2026-06-23 | 拆出右侧侧栏通用渲染片段 `apps/web/lib/sidebar/renderers.js` 与笔记统计 `apps/web/lib/sidebar/stats.js`，补充对应测试。 |
| 2026-06-23 | 新增右侧侧栏状态模块 `apps/web/lib/sidebar/state.js`，拆出当前笔记侧栏数据清空补丁。 |
| 2026-06-23 | 拆出左侧二级分组菜单渲染模块 `apps/web/lib/navigation/section-menu-renderers.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航右键菜单 HTML 渲染模块 `apps/web/lib/navigation/context-menu-renderers.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 将左侧导航右键菜单项规则迁入 `apps/web/lib/navigation/context-menu.js`，补充菜单项测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航点击目标解析模块 `apps/web/lib/navigation/click-target.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航右键菜单目标解析模块 `apps/web/lib/navigation/context-menu.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航拖拽落点与可投放判断模块 `apps/web/lib/navigation/drag-drop.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航选择与展开状态模块 `apps/web/lib/navigation/selection.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航树可见性选择器 `apps/web/lib/navigation/visibility.js`，补充对应测试与导航文档入口。 |
| 2026-06-22 | 拆出左侧导航树渲染模块 `apps/web/lib/navigation/tree-renderers.js`，补充导航目录与改动入口。 |
| 2026-06-22 | 创建项目结构导航，记录当前 monorepo、API、Web、重构后前端模块边界和后续拆分重点。 |
